// Config file for nextflow file

// Enable Specify docker container
process.container = 'john2018330/lobeliad_phylogenomics:0.0'
docker.enabled = true

// Allow docker to run with user permissions
// docker.runOptions = '-u $(id -u):$(id -g)'
docker.runOptions = '--user root'
